# Quote Calculator - Basic Auth Template
#
# This file contains username:password pairs for HTTP Basic Authentication
# Copy this file to .htpasswd and generate secure password hashes
#
# IMPORTANT: This is an EXAMPLE file. DO NOT use these credentials in production!
#
# ============================================================================
# How to generate secure passwords:
# ============================================================================
#
# Method 1: Using htpasswd command (Apache utils)
#   htpasswd -c .htpasswd username
#   (will prompt for password and create file)
#
# Method 2: Using htpasswd command (add more users)
#   htpasswd .htpasswd another_user
#
# Method 3: Using openssl
#   echo "username:$(openssl passwd -apr1 'your_password')" >> .htpasswd
#
# Method 4: Online generator
#   https://hostingcanada.org/htpasswd-generator/
#
# ============================================================================
# Example credentials (for testing only - CHANGE IN PRODUCTION):
# ============================================================================
#
# Username: admin
# Password: admin123
# Hash (bcrypt): $2y$10$...
admin:$apr1$ruca84Hq$mbjdMZBAG.KWn7vfN/SNK/

# Username: user
# Password: user123
# Hash (bcrypt): $2y$10$...
user:$apr1$r31d..H0$HqJZimcKQFAMYayBlzkrA/

# ============================================================================
# Enable Basic Auth in nginx config:
# ============================================================================
#
# 1. Copy this file to .htpasswd
# 2. Generate new secure password hashes
# 3. Uncomment these lines in nginx/conf.d/common-config.conf:
#
#    location /api/ {
#        auth_basic "Restricted Access";
#        auth_basic_user_file /etc/nginx/.htpasswd;
#        # ... rest of config
#    }
#
# 4. Mount .htpasswd in docker-compose:
#    volumes:
#      - ./nginx/.htpasswd:/etc/nginx/.htpasswd:ro
#
# ============================================================================
# Security Notes:
# ============================================================================
#
# - Never commit .htpasswd to git (it's in .gitignore)
# - Use strong, unique passwords (min 16 characters)
# - Change passwords regularly (every 90 days)
# - Consider using OAuth/JWT for production instead of basic auth
# - Basic auth sends credentials with every request (use HTTPS!)
# - For API access, use API keys or tokens instead
#
# ============================================================================
# Alternative: Environment-based auth
# ============================================================================
#
# For Docker environments, you can use environment variables:
#
# docker-compose.prod.yml:
#   environment:
#     - AUTH_USER=${AUTH_USER}
#     - AUTH_PASS=${AUTH_PASS}
#
# .env:
#   AUTH_USER=admin
#   AUTH_PASS=your_secure_password_here
#
