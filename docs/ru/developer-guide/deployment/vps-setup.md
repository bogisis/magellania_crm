# Настройка VPS Hostinger

Пошаговая инструкция по настройке VPS на Hostinger для развертывания Quote Calculator.

---

## 🎯 Обзор

Время настройки: **15-20 минут**

Что будет установлено:
- Ubuntu 22.04 LTS
- Docker & Docker Compose
- Portainer (Web UI для управления Docker)
- UFW Firewall
- Базовые меры безопасности

---

## 📦 Выбор VPS плана

### Рекомендуемая конфигурация

**Hostinger VPS Plan 2** - оптимальный выбор:

| Параметр | Значение | Примечание |
|----------|----------|------------|
| **CPU** | 2 vCores | Достаточно для prod + staging |
| **RAM** | 4 GB | Комфортный запас |
| **Storage** | 100 GB NVMe SSD | Быстрый диск |
| **Bandwidth** | 2 TB/месяц | Более чем достаточно |
| **OS** | Ubuntu 22.04 LTS | **Обязательно выбрать** |
| **Цена** | ~$5.99/мес | При годовой оплате |

!!! tip "Почему именно Ubuntu 22.04 LTS?"
    - ✅ Long Term Support до 2027 года
    - ✅ Официальная поддержка Docker
    - ✅ Проверенная совместимость с проектом
    - ✅ Огромное сообщество и документация
    - ✅ Легкая настройка и администрирование

### Альтернативные варианты

#### VPS Plan 1 (экономный)
- CPU: 1 vCore
- RAM: 2 GB
- **Подходит для:** только production БЕЗ staging
- **Ограничения:** может быть медленнее при нагрузках

#### VPS Plan 3 (расширенный)
- CPU: 4 vCores
- RAM: 8 GB
- **Подходит для:** высокие нагрузки, много пользователей
- **Стоимость:** ~$11.99/мес

---

## 🚀 Первоначальная настройка

### Шаг 1: Подключение к VPS

После заказа VPS вы получите:
- IP адрес
- Root пароль (на email)

```bash
# Подключение через SSH
ssh root@your-vps-ip

# При первом подключении подтвердите fingerprint
# Введите root пароль
```

!!! warning "Безопасность"
    Первым делом смените root пароль на надежный!

### Шаг 2: Обновление системы

```bash
# Обновить список пакетов
apt update

# Установить обновления
apt upgrade -y

# Перезагрузка (если нужно)
# reboot
```

### Шаг 3: Создание non-root пользователя

```bash
# Создать пользователя deployer
adduser deployer

# Введите пароль (запомните его!)
# Остальные поля можно оставить пустыми (Enter)

# Добавить в группу sudo
usermod -aG sudo deployer

# Проверить
groups deployer
# Output: deployer : deployer sudo
```

### Шаг 4: Настройка SSH ключей (рекомендуется)

**На локальной машине:**

```bash
# Генерация SSH ключа (если нет)
ssh-keygen -t ed25519 -C "your-email@example.com"

# Копирование ключа на VPS
ssh-copy-id deployer@your-vps-ip

# Тест подключения без пароля
ssh deployer@your-vps-ip
```

**На VPS (опционально - отключить root login):**

```bash
# Редактировать SSH конфигурацию
sudo nano /etc/ssh/sshd_config

# Найти и изменить:
# PermitRootLogin yes
# На:
PermitRootLogin no

# Ctrl+X, Y, Enter для сохранения

# Перезапустить SSH
sudo systemctl restart sshd
```

!!! danger "Внимание!"
    Убедитесь, что вы можете подключиться через `deployer` БЕЗ пароля перед отключением root login!

---

## 🐳 Установка Docker

### Автоматическая установка (рекомендуется)

```bash
# Переключиться на deployer (если еще root)
su - deployer

# Скачать и запустить скрипт установки Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавить deployer в группу docker
sudo usermod -aG docker deployer

# ВАЖНО: Выйти и войти заново для применения группы
exit
ssh deployer@your-vps-ip

# Проверка
docker --version
# Output: Docker version 24.0.x, build xxxxx
```

### Установка Docker Compose

```bash
# Docker Compose v2 устанавливается как plugin
sudo apt install docker compose-plugin -y

# Проверка
docker compose version
# Output: Docker Compose version v2.x.x
```

### Тест Docker

```bash
# Запустить тестовый контейнер
docker run hello-world

# Если видите "Hello from Docker!" - все работает!
```

---

## 🔒 Настройка Firewall (UFW)

### Включение и настройка

```bash
# Разрешить SSH (КРИТИЧЕСКИ ВАЖНО!)
sudo ufw allow 22/tcp

# Разрешить HTTP
sudo ufw allow 80/tcp

# Разрешить HTTPS
sudo ufw allow 443/tcp

# Разрешить Portainer
sudo ufw allow 9443/tcp

# Включить firewall
sudo ufw enable

# Подтвердить: y

# Проверка статуса
sudo ufw status verbose
```

Expected output:
```
Status: active

To                         Action      From
--                         ------      ----
22/tcp                     ALLOW       Anywhere
80/tcp                     ALLOW       Anywhere
443/tcp                     ALLOW       Anywhere
9443/tcp                    ALLOW       Anywhere
```

!!! danger "Критично!"
    Сначала разрешите порт 22 (SSH), ПОТОМ включайте firewall! Иначе потеряете доступ к серверу.

---

## 🎛️ Установка Portainer

Portainer - web-интерфейс для управления Docker контейнерами.

### Установка

```bash
# Создать volume для данных Portainer
docker volume create portainer_data

# Запустить Portainer
docker run -d \
  -p 9443:9443 \
  --name portainer \
  --restart=always \
  -v /var/run/docker.sock:/var/run/docker.sock \
  -v portainer_data:/data \
  portainer/portainer-ce:latest

# Проверка
docker ps | grep portainer
```

### Первый вход

1. Открыть в браузере: `https://your-vps-ip:9443`
2. **Создать админ-аккаунт** (только при первом входе!)
   - Username: admin
   - Password: (надежный пароль, минимум 12 символов)
3. Выбрать "Local" environment
4. Нажать "Connect"

!!! warning "Важно!"
    У вас есть **5 минут** на создание админ-аккаунта после запуска Portainer. Если не успеете - нужно пересоздать контейнер.

### Проверка Portainer

В Portainer вы должны увидеть:
- ✅ 1 container (portainer сам себя показывает)
- ✅ 1 volume (portainer_data)
- ✅ 1 network (bridge)

---

## 💾 Настройка Swap (опционально, но рекомендуется)

Для VPS с 4GB RAM создание swap файла поможет избежать out-of-memory ошибок.

```bash
# Создать 2GB swap файл
sudo fallocate -l 2G /swapfile

# Установить права
sudo chmod 600 /swapfile

# Создать swap
sudo mkswap /swapfile

# Включить swap
sudo swapon /swapfile

# Проверка
free -h
# Должна быть строка Swap: с 2.0G

# Сделать постоянным (автозапуск)
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab

# Проверка fstab
tail -1 /etc/fstab
```

---

## 🛠️ Установка дополнительных утилит

### Полезные пакеты

```bash
# Мониторинг и диагностика
sudo apt install -y \
  htop \          # Мониторинг процессов
  ncdu \          # Анализ дискового пространства
  iotop \         # Мониторинг I/O
  net-tools \     # Сетевые утилиты
  curl \          # HTTP клиент
  wget \          # Загрузка файлов
  git \           # Git
  vim \           # Текстовый редактор
  sqlite3 \       # SQLite CLI
  jq \            # JSON processor
  tree            # Визуализация директорий
```

### Настройка timezone

```bash
# Посмотреть текущий timezone
timedatectl

# Установить (пример: Москва)
sudo timedatectl set-timezone Europe/Moscow

# Или для Киева
# sudo timedatectl set-timezone Europe/Kiev

# Проверка
date
```

---

## 🔐 Дополнительные меры безопасности (опционально)

### Fail2ban

Защита от brute-force атак:

```bash
# Установка
sudo apt install fail2ban -y

# Запуск
sudo systemctl start fail2ban
sudo systemctl enable fail2ban

# Проверка статуса
sudo fail2ban-client status
```

### Автоматические обновления безопасности

```bash
# Установка
sudo apt install unattended-upgrades -y

# Настройка
sudo dpkg-reconfigure --priority=low unattended-upgrades

# Выбрать: Yes
```

### Изменение SSH порта (опционально)

!!! warning "Для опытных пользователей"
    Изменение порта SSH может усложнить подключение. Делайте только если понимаете последствия.

```bash
# Редактировать SSH конфигурацию
sudo nano /etc/ssh/sshd_config

# Найти строку:
# Port 22

# Изменить на (например):
Port 2222

# Сохранить (Ctrl+X, Y, Enter)

# Разрешить новый порт в firewall
sudo ufw allow 2222/tcp

# Перезапустить SSH
sudo systemctl restart sshd

# Тест подключения (НЕ закрывая текущую сессию!)
# В новом терминале:
ssh -p 2222 deployer@your-vps-ip

# Если работает, удалить старое правило
sudo ufw delete allow 22/tcp
```

---

## ✅ Проверка готовности VPS

### Чеклист

Убедитесь что все выполнено:

- [ ] VPS доступен по SSH
- [ ] Создан non-root пользователь `deployer`
- [ ] SSH ключи настроены (опционально, но рекомендуется)
- [ ] Docker установлен и работает
- [ ] Docker Compose установлен
- [ ] UFW firewall настроен и активен
- [ ] Portainer запущен и доступен на порту 9443
- [ ] Swap файл создан (опционально)
- [ ] Timezone настроен правильно

### Тестовые команды

```bash
# 1. Проверка Docker
docker --version
docker compose version
docker ps

# 2. Проверка сети
curl -I https://google.com

# 3. Проверка firewall
sudo ufw status

# 4. Проверка дискового пространства
df -h

# 5. Проверка памяти
free -h

# 6. Проверка Portainer
curl -k https://localhost:9443
```

Все команды должны выполниться без ошибок.

---

## 📊 Системная информация

### Полезные команды мониторинга

```bash
# CPU и память в реальном времени
htop

# Дисковое пространство
df -h
ncdu /

# Сетевые подключения
sudo netstat -tulpn

# Логи системы
sudo journalctl -xe
sudo tail -f /var/log/syslog

# Docker статистика
docker stats
```

---

## 🆘 Troubleshooting

### Проблема: Не могу подключиться по SSH

**Решение:**
```bash
# Проверить что SSH сервис запущен (через Hostinger панель)
# Проверить firewall:
sudo ufw status

# Если SSH заблокирован, через Hostinger VNC console:
sudo ufw allow 22/tcp
```

### Проблема: Docker permission denied

**Решение:**
```bash
# Добавить пользователя в группу docker
sudo usermod -aG docker $USER

# Выйти и войти заново
exit
ssh deployer@your-vps-ip

# Проверка
docker ps
```

### Проблема: Portainer не доступен

**Решение:**
```bash
# Проверить что контейнер запущен
docker ps | grep portainer

# Проверить firewall
sudo ufw status | grep 9443

# Проверить логи
docker logs portainer

# Перезапустить контейнер
docker restart portainer
```

### Проблема: Out of memory

**Решение:**
```bash
# Создать swap файл (см. раздел выше)
# Проверить потребление памяти
free -h
docker stats

# Найти прожорливые процессы
htop
```

---

## 🎯 Следующие шаги

VPS готов! Теперь можно переходить к развертыванию приложения:

1. **[Конфигурация →](configuration.md)** - Настройка .env файлов и Nginx
2. **[Развертывание приложения →](production.md#cloud-deployment)**
3. **[SSL сертификаты →](ssl.md)**

---

## 📚 Дополнительные ресурсы

- [Docker Documentation](https://docs.docker.com/)
- [Ubuntu Server Guide](https://ubuntu.com/server/docs)
- [Portainer Documentation](https://docs.portainer.io/)
- [UFW Documentation](https://help.ubuntu.com/community/UFW)
- [Hostinger VPS Tutorials](https://www.hostinger.com/tutorials/vps)

---

[← Назад к Deployment](index.md) | [Конфигурация →](configuration.md)
