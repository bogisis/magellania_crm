# Системные требования

**Версия:** 2.3.0
**Дата обновления:** 6 ноября 2025

## Обзор

Quote Calculator v2.3.0 - это легковесное веб-приложение с минимальными системными требованиями. Работает на большинстве современных систем.

---

## Минимальные требования

### Серверная часть (Backend)

| Компонент | Минимум | Рекомендуется |
|---|---|---|
| **CPU** | 1 core | 2+ cores |
| **RAM** | 512 MB | 1 GB |
| **Дисковое пространство** | 100 MB | 500 MB |
| **Node.js** | 16.0.0 | 18.x LTS |
| **npm** | 8.0.0 | 9.x |
| **OS** | Linux, macOS, Windows 10+ | Linux Server |

### Клиентская часть (Frontend)

| Браузер | Минимальная версия |
|---|---|
| **Google Chrome** | 90+ |
| **Mozilla Firefox** | 88+ |
| **Safari** | 14+ |
| **Microsoft Edge** | 90+ |

**Не поддерживается:** Internet Explorer

---

## Node.js

### Установка Node.js

**macOS (Homebrew):**
```bash
# Установить последнюю LTS версию
brew install node@18

# Проверить версию
node --version
npm --version
```

**Linux (Ubuntu/Debian):**
```bash
# Добавить репозиторий NodeSource
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -

# Установить Node.js
sudo apt-get install -y nodejs

# Проверить версию
node --version
npm --version
```

**Windows:**
1. Скачать установщик с https://nodejs.org/
2. Выбрать LTS версию (18.x)
3. Запустить установщик
4. Проверить установку в командной строке:
   ```cmd
   node --version
   npm --version
   ```

### Альтернатива: nvm (Node Version Manager)

Рекомендуется для разработчиков:

**macOS/Linux:**
```bash
# Установить nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# Перезагрузить terminal, затем:
nvm install 18
nvm use 18
nvm alias default 18

# Проверить
node --version
```

**Windows:**
```powershell
# Скачать nvm-windows
https://github.com/coreybutler/nvm-windows/releases

# После установки:
nvm install 18
nvm use 18
```

---

## Docker (опционально)

Если планируете использовать Docker установку:

### Установка Docker

**macOS:**
```bash
# Скачать Docker Desktop
https://www.docker.com/products/docker-desktop

# Или через Homebrew
brew install --cask docker

# Проверить
docker --version
docker-compose --version
```

**Linux (Ubuntu/Debian):**
```bash
# Установить Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавить пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker

# Установить Docker Compose
sudo apt-get install docker-compose-plugin

# Проверить
docker --version
docker compose version
```

**Windows:**
```bash
# Скачать Docker Desktop для Windows
https://www.docker.com/products/docker-desktop
```

### Требования Docker

| Компонент | Минимум | Рекомендуется |
|---|---|---|
| **Docker** | 20.10.0 | 24.x |
| **Docker Compose** | 2.0.0 | 2.x |
| **RAM для Docker** | 2 GB | 4 GB |
| **Диск для Docker** | 10 GB | 20 GB |

---

## Дисковое пространство

### Разбивка по компонентам

```
quote-calculator/
├── node_modules/        ~100 MB  # Зависимости Node.js
├── db/
│   └── quotes.db       ~10-50 MB # База данных (растет с данными)
├── estimate/           ~1-10 MB  # Legacy JSON сметы
├── catalog/            ~1-5 MB   # Каталоги шаблонов
├── backup/             ~5-50 MB  # Автоматические backups
├── logs/               ~1-10 MB  # Логи сервера
├── site/               ~20 MB    # MkDocs документация (опционально)
└── Исходники           ~5 MB     # JavaScript, HTML, CSS

ИТОГО:                  ~143-252 MB
```

### Рекомендации по диску

**Минимум:** 100 MB свободного места
**Рекомендуется:** 500 MB для комфортной работы
**Продакшн:** 2-5 GB с учетом роста данных и backups

### Проверка свободного места

**macOS/Linux:**
```bash
df -h .
```

**Windows:**
```cmd
dir
```

---

## Память (RAM)

### Потребление памяти

| Режим | RAM usage |
|---|---|
| **Idle** (без запросов) | ~100 MB |
| **Нормальная работа** | ~150-200 MB |
| **Пиковая нагрузка** | ~300-400 MB |
| **Большие exports** | ~500 MB |

### Рекомендации

**Минимум:** 512 MB свободной RAM
**Рекомендуется:** 1 GB для комфортной работы
**Продакшн:** 2 GB с запасом

### Проверка доступной памяти

**macOS:**
```bash
vm_stat | perl -ne '/page size of (\d+)/ and $size=$1; /Pages\s+([^:]+)[^\d]+(\d+)/ and printf("%-16s % 16.2f Mi\n", "$1:", $2 * $size / 1048576);'
```

**Linux:**
```bash
free -h
```

**Windows:**
```powershell
Get-CimInstance Win32_OperatingSystem | Select-Object FreePhysicalMemory
```

---

## Процессор (CPU)

### Требования

**Минимум:** 1 CPU core
**Рекомендуется:** 2+ CPU cores
**Продакшн:** 4+ CPU cores для нагруженных систем

### Проверка CPU

**macOS/Linux:**
```bash
# Количество ядер
nproc

# Детальная информация
lscpu  # Linux
sysctl -n hw.ncpu  # macOS
```

**Windows:**
```powershell
Get-WmiObject Win32_Processor | Select-Object NumberOfCores,NumberOfLogicalProcessors
```

---

## Сеть

### Порты

По умолчанию используется **порт 4000**.

Убедитесь, что порт свободен:

**macOS/Linux:**
```bash
lsof -i :4000
```

**Windows:**
```cmd
netstat -ano | findstr :4000
```

Если порт занят, измените его через переменную окружения:
```bash
PORT=3000 npm start
```

### Требования к сети

- **Локальная работа:** нет требований к интернету
- **Docker installation:** требуется интернет для скачивания образов (~200 MB)
- **npm install:** требуется интернет для скачивания пакетов (~100 MB)

### Firewall

Если используете firewall, разрешите входящие подключения на порт 4000:

**Linux (ufw):**
```bash
sudo ufw allow 4000/tcp
```

**macOS (pf):**
```bash
# Добавить в /etc/pf.conf
pass in proto tcp to port 4000
```

**Windows Firewall:**
```powershell
New-NetFirewallRule -DisplayName "Quote Calculator" -Direction Inbound -LocalPort 4000 -Protocol TCP -Action Allow
```

---

## База данных

### SQLite (по умолчанию)

**Версия:** 3.35.0 или выше (встроена в Node.js)
**Файл:** `db/quotes.db`
**Размер:** растет с данными (обычно 10-50 MB)

### Альтернативные хранилища

В будущих версиях планируется поддержка:
- PostgreSQL
- MySQL
- MongoDB

---

## Браузеры

### Поддерживаемые браузеры

| Браузер | Версия | Поддержка |
|---|---|---|
| **Chrome** | 90+ | ✅ Полная |
| **Firefox** | 88+ | ✅ Полная |
| **Safari** | 14+ | ✅ Полная |
| **Edge** | 90+ | ✅ Полная |
| **Opera** | 76+ | ⚠️ Частичная |
| **IE 11** | - | ❌ Не поддерживается |

### Требуемые возможности браузера

- ✅ ES6+ JavaScript
- ✅ CSS Custom Properties (variables)
- ✅ Fetch API
- ✅ LocalStorage
- ✅ Print API
- ✅ File API

### Проверка совместимости

Откройте в браузере: `http://localhost:4000`

Если видите интерфейс приложения - ваш браузер поддерживается.

---

## Операционные системы

### Полная поддержка

✅ **macOS** 10.15 (Catalina) и выше
✅ **Linux:**
  - Ubuntu 20.04+
  - Debian 11+
  - CentOS 8+
  - Fedora 35+
  - Arch Linux (latest)
✅ **Windows** 10 и 11

### Частичная поддержка

⚠️ **Windows 8.1** - работает, но не тестировалось
⚠️ **macOS 10.14** - работает, но не рекомендуется

### Не поддерживается

❌ **Windows 7** и ниже
❌ **macOS 10.13** и ниже

---

## Дополнительные инструменты (опционально)

### Для разработки

- **Git** - для клонирования репозитория и version control
- **curl** - для тестирования API
- **jq** - для работы с JSON в командной строке
- **sqlite3** - для просмотра базы данных

### Установка дополнительных инструментов

**macOS:**
```bash
brew install git curl jq sqlite3
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get install git curl jq sqlite3
```

**Windows:**
- **Git:** https://git-scm.com/download/win
- **curl:** встроен в Windows 10+
- **jq:** https://stedolan.github.io/jq/download/
- **sqlite3:** https://www.sqlite.org/download.html

---

## Production требования

Для продакшн окружения рекомендуется:

### Сервер

| Компонент | Рекомендуется |
|---|---|
| **CPU** | 2-4 cores |
| **RAM** | 2-4 GB |
| **Диск** | 10-20 GB SSD |
| **Сеть** | 100 Mbps+ |
| **Uptime** | 99.9%+ |

### Дополнительные компоненты

- ✅ **Nginx** - reverse proxy и SSL termination
- ✅ **Let's Encrypt** - бесплатные SSL сертификаты
- ✅ **PM2** - process manager для Node.js (альтернатива Docker)
- ✅ **Backup система** - автоматические backups данных
- ✅ **Monitoring** - Prometheus + Grafana

### SSL/TLS

Для продакшена обязательно используйте HTTPS:

```bash
# Установить certbot
sudo apt-get install certbot

# Получить сертификат
sudo certbot certonly --standalone -d your-domain.com
```

---

## Проверка готовности системы

Используйте этот чеклист перед установкой:

```bash
#!/bin/bash
# check-requirements.sh

echo "=== Проверка системных требований ==="
echo ""

# Node.js
if command -v node &> /dev/null; then
    NODE_VERSION=$(node --version)
    echo "✅ Node.js: $NODE_VERSION"
else
    echo "❌ Node.js не установлен"
fi

# npm
if command -v npm &> /dev/null; then
    NPM_VERSION=$(npm --version)
    echo "✅ npm: v$NPM_VERSION"
else
    echo "❌ npm не установлен"
fi

# Git
if command -v git &> /dev/null; then
    GIT_VERSION=$(git --version)
    echo "✅ $GIT_VERSION"
else
    echo "⚠️  Git не установлен (опционально)"
fi

# Docker (опционально)
if command -v docker &> /dev/null; then
    DOCKER_VERSION=$(docker --version)
    echo "✅ $DOCKER_VERSION"
else
    echo "⚠️  Docker не установлен (опционально)"
fi

# Свободное место
echo ""
echo "Свободное место на диске:"
df -h . | tail -1

# Свободная память
echo ""
echo "Свободная память:"
free -h | grep Mem

echo ""
echo "=== Проверка завершена ==="
```

Запустите:
```bash
chmod +x check-requirements.sh
./check-requirements.sh
```

---

## FAQ

### Нужен ли мне Docker?

**Нет**, Docker опционален. Локальная установка проще для начала.

### Какую версию Node.js использовать?

**Рекомендуется Node.js 18.x LTS** - стабильная версия с долгосрочной поддержкой.

### Можно ли запустить на Raspberry Pi?

**Да**, но с ограничениями. Рекомендуется модель 4 с 4GB RAM.

### Работает ли на Windows?

**Да**, Quote Calculator полностью поддерживает Windows 10 и 11.

### Нужен ли PostgreSQL/MySQL?

**Нет**, Quote Calculator использует встроенный SQLite. PostgreSQL/MySQL поддержка планируется в будущих версиях.

### Сколько пользователей поддерживается?

**Один пользователь** на инстанс. Для multi-user нужна дополнительная настройка.

---

## Что дальше?

После проверки требований:

1. 📖 **[Локальная установка](local.md)** - простая установка
2. 🐳 **[Docker установка](docker.md)** - продакшн установка
3. 🚀 **[Быстрый старт](../getting-started/first-estimate.md)** - первая смета

---

**Предыдущий шаг:** [← Docker установка](docker.md)
**Следующий шаг:** [Начало работы →](../getting-started/first-estimate.md)
